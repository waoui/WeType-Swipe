# 微信输入法下滑快捷键 v1.11.6-test1

## 新增功能

本测试版解决“内置模块后没有独立模块 APP，无法进入设置”的问题。

- 在微信输入法“关于”界面底部增加 **下滑快捷键设置** 入口。
- 点击后直接在微信输入法当前进程内打开完整的内置设置界面，不依赖独立模块 APK、桌面图标或 LSPosed 模块设置入口。
- 内置设置支持：
  - 26 键动作绑定；
  - 九宫格 2–9 动作绑定；
  - 长按按键设置自动／自定义／隐藏标签；
  - 26 键与九宫格触发距离；
  - 按键功能文字、下滑提示、模块震动开关；
  - 恢复默认、清空 26 键。
- 保存后直接写入微信输入法自身的模块缓存，并通过配置广播同步到同包名的其他进程，立即生效。
- 内置模式不显示“隐藏桌面图标”选项，因为不存在独立模块桌面入口。

## 关于页识别

为兼容微信输入法不同版本／不同页面实现，入口采用两条识别链路：

1. Activity 恢复时扫描“关于微信输入法”页面；
2. 关于页标题 View 动态挂载时再次触发识别。

入口使用独立底部卡片覆盖层，不依赖微信输入法具体列表类名和混淆 View 层级，降低版本升级导致入口失效的概率。

## 真机验收

1. **独立模块模式**
   - 安装测试 APK、LSPosed 正常启用。
   - 原模块设置页仍能正常使用、保存和应用。

2. **内置模块模式（重点）**
   - 不安装独立模块 APK，仅保留内置模块。
   - 打开微信输入法设置 → 关于。
   - 页面底部应出现“下滑快捷键设置 / 内置模块设置”入口。
   - 点击后应打开完整设置页，不闪退、不跳转到不存在的模块 APK。
   - 修改一个 26 键动作并保存，立即回到输入框验证生效。
   - 修改一个九宫格动作并保存，验证生效。
   - 修改触发距离、功能文字、触发提示、震动，验证均生效。
   - 强制停止并重新打开微信输入法，配置仍保留。

3. **回归**
   - 关于页以外不应出现模块设置入口。
   - 数字、符号、手写键盘和密码框隔离不变。
   - 文首／文尾、选至文首／文尾、撤销／重做、剪贴板、快捷发送均无回归。
   - 正常点击、长按、滑行输入不受影响。

## 自动验证

构建任务执行：

```bash
./gradlew --no-daemon --stacktrace clean testDebugUnitTest lintDebug assembleRelease
```

并校验项目永久发布证书，输出 APK、源码 ZIP、测试说明和 SHA-256 文件。
