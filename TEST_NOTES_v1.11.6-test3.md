# 微信输入法下滑快捷键 v1.11.6-test3

## 本版修复

前两版没有触发的根因已调整：不再依赖 `Activity.dispatchTouchEvent`、Activity 名称、原生 TextView 或关于页布局识别。

本版把隐藏设置入口接入模块现有、真机已经验证工作的 `View.dispatchTouchEvent` 全局 Hook。该 Hook 当前就是 26 键/九宫格下滑识别所使用的触摸链路，因此不再新增一条未经验证的页面触摸链。

## 触发方式

1. 打开微信输入法设置 → 关于。
2. 在关于页上半部分，对微信输入法图标所在位置连续点击 7 次。
3. 第 7 次后打开完整的“下滑快捷键设置”。

触发规则：
- 两次点击间隔超过 2 秒，重新计数；
- 点击位置明显偏离第一次点击位置，重新计数；
- 同一个物理 `ACTION_UP` 即使经过父子多个 View，也只计一次；
- 不再把“能否读取到关于页文字”作为触发前提，因此自绘/Compose 页面也可工作；
- 触发区域限制在窗口上半部，降低其它页面误触概率。

## 内置设置

支持：
- 26 键动作绑定；
- 九宫格 2–9 动作绑定；
- 自动/自定义/隐藏按键标签；
- 26 键和九宫格触发距离；
- 功能文字、下滑提示、震动；
- 恢复默认、清空 26 键；
- 保存后写入微信输入法自身模块缓存并同步其它进程。

## 版本

- VERSION_NAME: `1.11.6-test3`
- VERSION_CODE: `47`
- 使用项目永久发布证书签名。

## 自动验证

- `AboutIconUnlockTest`
  - 第 7 次触发；
  - 同一物理事件多次分发只计一次；
  - 超过 2 秒重置；
  - 点击位置变化重置；
  - 时钟回退重置。
- `EmbeddedConfigEditorTest`
- `ConfigSnapshotTest`
- Gradle 单元测试、Lint、Release 构建；
- 永久签名证书校验。

## 真机重点

本轮先只确认第一件事：

**微信输入法 → 设置 → 关于 → 连续点击图标位置 7 次，是否能打开设置界面。**

如果能打开，再继续验证保存配置和立即生效。
