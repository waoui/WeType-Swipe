# 微信输入法下滑快捷键 v1.11.6-test2

## 调整方案

`v1.11.6-test1` 在“关于”页插入可见入口的方案真机无效，本版改为隐藏触发，不再修改微信输入法原生页面布局。

- 打开微信输入法 **设置 → 关于**。
- 对关于页中的 **微信输入法图标连续点击 7 次**。
- 第 7 次点击后，直接在微信输入法当前进程内打开“下滑快捷键设置”。
- 点击图标原本的行为不被替换；模块只旁路监听点击坐标。
- 两次点击间隔超过 2 秒会自动重新计数；中途点击图标区域以外的位置也会重新计数。

## 为什么改成这个方案

- 不再向关于页插入 View，因此不依赖原生页面容器结构、列表实现或底部布局。
- 不需要独立模块 APK、桌面图标或 LSPosed 模块设置 Activity。
- 关于页识别使用页面文字、Activity 名称和版本／隐私等辅助特征组合判断。
- 图标命中优先识别实际 ImageView；无法可靠定位时，回退到关于页上半部分中央图标区域。
- 只有在确认处于关于页且连续 7 次点击图标区域时才会触发，避免其它页面误开设置。

## 内置设置能力

- 26 键动作绑定；
- 九宫格 2–9 动作绑定；
- 自动／自定义／隐藏按键标签；
- 26 键与九宫格触发距离；
- 按键功能文字、下滑提示、模块震动；
- 恢复默认、清空 26 键；
- 保存后直接写入微信输入法自身模块缓存，并广播到同包名其它进程立即生效。

## 真机验收

1. 仅保留内置模块，不依赖独立模块 APK。
2. 打开微信输入法设置 → 关于。
3. 连续点击页面中的微信输入法图标 7 次。
4. 第 7 次后应直接打开完整的“下滑快捷键设置”界面。
5. 在关于页以外连续点击图片／图标，不应触发设置。
6. 点击 3 次后停顿超过 2 秒，再继续点击，不应累计前面的次数。
7. 修改一个 26 键动作并保存，回输入框验证立即生效。
8. 修改九宫格、触发距离、功能文字、提示或震动后保存，验证生效。
9. 强制停止并重新打开微信输入法，配置应保留。
10. 文首／文尾、撤销／重做、剪贴板、快捷发送、数字／符号／手写／密码框隔离均无回归。

## 自动验证

构建执行：

```bash
./gradlew --no-daemon --stacktrace clean testDebugUnitTest lintDebug assembleRelease
```

新增 `AboutIconUnlockTest` 验证连续七击、超时重置和时钟回退重置，并校验项目永久发布证书。
