# 微信输入法下滑快捷键 v1.11.5-test1

## 新增功能

本测试版独立新增 4 个全文级动作，不修改现有段落级动作语义：

- **文首**：光标移动到整个编辑文本最前面。
- **文尾**：光标移动到整个编辑文本最后面。
- **选至文首**：从当前光标或已有选区的右边界，向前扩展到全文开头。
- **选至文尾**：从当前光标或已有选区的左边界，向后扩展到全文结尾。

## 保持不变

- 段首、段尾、选至段首、选至段尾继续按换行符划分当前段落。
- 原有动作编号 0–15 不变；新增动作使用 16–19，兼容已有配置。
- 数字、符号、手写键盘和密码输入框的隔离逻辑不变。
- 模块不保存、上传或记录用户输入内容。

## 实现边界

- 文首和选至文首可直接使用绝对文本位置 0。
- 文尾和选至文尾只在目标编辑器返回完整文本范围后执行。
- 如目标 App 使用自绘编辑器且拒绝返回完整文本，动作会明确失败，不会把 65536 字符上下文误当成全文结尾。

## 真机验收

1. 在 26 键和九宫格中分别确认新增 4 个动作都可绑定、保存并显示标签。
2. 在微信聊天输入框输入至少 3 行文字，把光标放在中间行：
   - 文首应移动到第一行第一个字符之前；
   - 文尾应移动到最后一行最后一个字符之后；
   - 选至文首应跨行选中到全文开头；
   - 选至文尾应跨行选中到全文结尾。
3. 先手动选中中间一段文字，再测试选至文首、选至文尾，确认已有选区另一侧边界被保留。
4. 测试空文本、单行文本、末尾带换行、多段文本和较长文本。
5. 检查段首、段尾、选至段首、选至段尾仍只处理当前段落。
6. 检查复制、剪切、粘贴、全选、撤销、重做、剪贴板和快捷发送无回归。
7. 检查数字／符号页面和手写找字界面不显示或触发这些动作。

## 自动验证

构建任务执行：

```bash
./gradlew --no-daemon --stacktrace clean testDebugUnitTest lintDebug assembleRelease
```

同时验证：

- 新动作编号与既有配置兼容；
- 26 键和九宫格菜单映射可往返；
- 折叠光标与已有选区的全文选择边界；
- 索引越界时的安全夹取；
- Release APK 使用项目永久签名证书。
