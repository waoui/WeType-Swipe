# WeType-Swipe v1.11.6-test5 真机测试说明

本测试包基于 v1.11.6-test4：保留关于页图标连续 5 击打开内置设置，以及内置/独立设置界面统一；新增“输入指定内容”。

## 新增功能

- 26 键和九宫格 2–9 的动作列表新增“输入指定内容”。
- 选择后可为当前按键填写独立文本，最多 1000 个 Unicode 字符。
- 支持中文、英文、数字、符号、Emoji 和换行。
- 多个按键可同时绑定“输入指定内容”，每个按键内容互不影响。
- 默认按键标签显示“文本”，仍可通过长按按键自定义/隐藏标签。
- 执行时先结束当前 composing 状态，再通过 InputConnection.commitText() 直接写入，不占用系统剪贴板。
- 密码输入框沿用现有安全限制，不执行模块快捷动作。

## 建议验收

1. 微信输入法 → 设置 → 关于 → 同一图标位置连续点击 5 次，确认内置设置可打开。
2. 26 键：例如 A 绑定“你好😀”，S 绑定“123”，保存后分别下滑，确认输入内容独立且正确。
3. 九宫格：例如 2 绑定一段固定回复，3 绑定多行文本，确认都可输入。
4. 输入拼音尚未上屏时触发指定内容，确认不会与原 composing 拼音混在一起。
5. 修改指定内容后保存，重新进入设置确认内容仍保留。
6. 强制停止/重启微信输入法后再次测试，确认配置持久化。

## 版本

- VERSION_NAME: 1.11.6-test5
- VERSION_CODE: 49
- 仍为测试版，不发布正式 Release，不同步 LSPosed 官方仓库。
